<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.Holo.Light">
        <item name="android:titleTextStyle">@color/pinki</item>
        <item name="android:textColor">@color/pinki</item>
        <!-- Customize your theme here. -->
    </style>

</resources>
